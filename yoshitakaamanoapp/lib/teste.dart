import 'package:flutter/material.dart';

class TestePag extends StatelessWidget {
  const TestePag ({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          children: [
            const Text('lalala'),
            Image.asset('')
          ],
        ),
      ),
    );
  }
}